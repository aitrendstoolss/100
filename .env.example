# Database URL for Prisma (SQLite connection)
# For SQLite, the path is resolved relative to the prisma directory
DATABASE_URL="file:./dev.db"

# Session Security Key (Change this to a random secure string in production)
# Example: openssl rand -base64 32
SESSION_SECRET="your_cryptographically_secure_random_session_secret"
